{
  "devDependencies": {
    "@types/node": "^22.17.2",
    "eslint": "^9.33.0",
    "jiti": "^2.5.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.40.0"
  },
  "name": "json-ts",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "format": "npm run format --workspaces",
    "lint": "npm run lint --workspaces",
    "start:api": "npm start --workspace=packages/api-hono",
    "start:cli": "npm start --workspace=packages/cli"
  },
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ]
}
